一、简答
1、列举三个邮件访问协议
2、作为互联设备，路由器和交换机各有何优缺点
3、Web服务，不知道URL对应的IP，要运行什么应用层和传输层协议（大概就这意思）
4、传输层和链路层的可靠数据传输有何异同，能否互相替代
5、为什么说TCP拥塞控制是加性增乘性减的
6、位于北京的节点想用ARP解析位于广州的IP，会发生什么，为什么

二、以太网
100Base-T，A和B用三个集线器（每个附加时延20bit time）连接，四根网线都是100m，线里面电磁波传播速度2*10^8m/s
1、以太网为什么要最大、最小帧长限制，最大最小帧长各是多少
2、从A到B的单向传播时延是多少，相当于多少bit time
3、A发送一个帧，未完成前B也开始发送，问A检测到B的发送时是否已经完成传输
4、把集线器都换成交换机，存储转发，每个处理时延20bit time，全双工，问A到B一个分组完整到达要多少时间，问在发送完成前是否会碰撞、重传

三、多路访问协议
TDMA   CSMA/CD   时隙ALOHA    Token passing
1、IP电话应用，用什么，为什么
2、偶发数据交换，用什么，为什么
3、两个都要，IP电话优先，用哪个，或者在哪个上面改，或者哪几个拼拼，理由

四、路由访问
某自治系统，R1、R2、R3三个路由器相连，R2和R3和别的自治系统连接，R1上另外两个端口连两个子网，一个上面有HTTP服务器S1和DNS服务器S2，另一个上面有客户机C1
1、C1访问S1上的一个URL，其中嵌一个外网的URL，问这些主机和路由器谁会发起两个DNS查询，（迭代）S2不知道外网URL对应的IP，问谁向外网发起DNS查询
2、六个节点谁必须要运行TCP，谁必须要运行UDP，为什么
3、S1和S2的子网上有10台服务器，C1的子网上有45台主机，给一块地址166.111.73.0/24，分配地址，尽量节省
4、谁要运行自治系统内部路由协议
5、谁要运行自治系统间路由协议，由3的答案给出该自治系统（R2和R3）向外界通告的前缀
6、路由器间点对点协议，两个子网用以太网跟路由器连接，问谁必须要运行ARP协议

五、滑动窗口（要求时间图，就那个来来回回的图，书上很多的那种）
1、SR，窗口大小和需要空间都相同（设为2），说明为什么会出错
2、GBN，发送方base=X，问是否会收到序号小于X的ACK

六、选路算法
1、LS算法，图参见07年版本，出发节点改成A
2、DV算法，Z和X、Y相连，X和Y向Z通告的表包括他们到f和g的距离（都有三个前一跳，就是一共六个数），Z初始化，问Z的表（一共四个数），具体数字不记得了

--

